// data/categories/testing.js
import { DIFFICULTY } from '../difficulty.js';

const { beginner, intermediate, advanced } = DIFFICULTY;

export const testing = [
  // ---------------------------------------------------------------- BEGINNER
  {
    id: 'test-types',
    difficulty: beginner,
    question: {
      ar: 'ما الفرق بين Unit و Integration و E2E tests؟',
      en: 'What is the difference between unit, integration and E2E tests?',
    },
    answer: {
      ar: '• Unit: يختبر وحدة واحدة معزولة — دالة أو مكوّن — مع استبدال اعتمادياتها. سريع جدًا (ميلي ثوانٍ) لكنه لا يضمن أن الأجزاء تعمل معًا.\n\n• Integration: يختبر تفاعل عدة أجزاء معًا — مكوّن مع الـ context ومع طبقة API وهمية، أو endpoint مع قاعدة بيانات اختبار. أبطأ لكنه يكشف أخطاء التكامل الحقيقية.\n\n• E2E: يختبر التطبيق كاملًا من منظور المستخدم عبر متصفح حقيقي. الأبطأ والأكثر هشاشة، لكنه الوحيد الذي يؤكد أن كل شيء يعمل فعلًا.\n\nهرم الاختبارات التقليدي يقترح: قاعدة عريضة من اختبارات الوحدات، ثم تكامل أقل، ثم قمة صغيرة من E2E.\n\nلكن الرأي الحديث الأكثر واقعية للواجهات الأمامية هو "كأس المثلجات المقلوب" (Testing Trophy) الذي يقترحه Kent C. Dodds: الثقل الأكبر على اختبارات التكامل، لأنها توفّر أفضل نسبة بين الثقة والتكلفة.\n\nلماذا؟ لأن اختبار وحدة لمكوّن React معزول تمامًا مع mock لكل شيء غالبًا يختبر تنفيذك لا سلوكك — فيمر رغم أن الميزة مكسورة، ويفشل عند إعادة هيكلة لا تغيّر السلوك.\n\nقاعدتي العملية: اختبر السلوك من منظور المستخدم، عند أعلى مستوى تستطيع تحمّل سرعته.',
      en: '• Unit: tests one isolated unit — a function or component — with its dependencies replaced. Very fast (milliseconds) but proves nothing about the parts working together.\n\n• Integration: tests several parts interacting — a component with its context and a mocked API layer, or an endpoint against a test database. Slower, but it catches real integration bugs.\n\n• E2E: tests the whole application from the user\'s perspective through a real browser. Slowest and most brittle, but the only kind that confirms everything actually works.\n\nThe classic test pyramid suggests a broad base of unit tests, fewer integration tests, and a small E2E cap.\n\nThe more realistic modern view for frontends is Kent C. Dodds\'s "Testing Trophy": put the most weight on integration tests, because they give the best confidence-to-cost ratio.\n\nWhy? Because a unit test for a fully isolated React component with everything mocked usually tests your implementation rather than your behaviour — it passes while the feature is broken, and fails on a refactor that changed no behaviour.\n\nMy practical rule: test behaviour from the user\'s perspective, at the highest level whose speed you can afford.',
    },
  },
  {
    id: 'test-what-to-test',
    difficulty: beginner,
    question: {
      ar: 'ما الذي يجب اختباره وما الذي لا يستحق الاختبار؟',
      en: 'What should you test, and what is not worth testing?',
    },
    answer: {
      ar: 'ما يستحق الاختبار بالتأكيد:\n1. منطق العمل: الحسابات، والقواعد، والتحقق من الصلاحيات — هنا تقع الأخطاء المكلفة.\n2. المسارات الحرجة: تسجيل الدخول، الدفع، الإرسال، أي شيء يفقد المال أو البيانات إن انكسر.\n3. الحالات الحدية: قائمة فارغة، قيمة صفرية، نص طويل جدًا، شبكة منقطعة.\n4. معالجة الأخطاء — الجزء الذي لا يُختبر يدويًا أبدًا.\n5. كل خطأ يُكتشف في الإنتاج: اكتب اختبارًا يعيد إنتاجه قبل إصلاحه، فلا يعود.\n\nما لا يستحق:\n• تفاصيل التنفيذ: اسم متغير حالة داخلي، أو عدد مرات استدعاء دالة داخلية. هذه تكسر الاختبارات عند إعادة الهيكلة دون أن تكشف خطأ حقيقيًا.\n• كود الطرف الثالث: المكتبات مختبرة عند أصحابها.\n• getters وsetters بسيطة بلا منطق.\n• التصميم البصري الدقيق — إلا عبر اختبارات لقطات بصرية مخصصة إن كان مهمًا.\n\nالسؤال الذي أستخدمه كمعيار: إن انكسر هذا، هل سيلاحظ المستخدم؟ إن كان الجواب لا، فالاختبار على الأرجح لا قيمة له.\n\nونقطة عن التغطية: نسبة 100% ليست هدفًا. تغطية عالية من اختبارات تختبر التنفيذ أسوأ من تغطية 60% تغطي المسارات المهمة بجودة. التغطية تكشف ما لم يُختبر إطلاقًا، لكنها لا تثبت جودة ما اختُبر.',
      en: 'What is definitely worth testing:\n1. Business logic: calculations, rules, permission checks — where the expensive bugs live.\n2. Critical paths: login, checkout, submission, anything that loses money or data when it breaks.\n3. Edge cases: empty lists, zero values, very long strings, a dropped network.\n4. Error handling — the part nobody ever tests manually.\n5. Every bug found in production: write a test reproducing it before fixing it, so it cannot return.\n\nWhat is not worth it:\n• Implementation details: the name of an internal state variable, or how many times an internal function was called. These break on refactors without revealing real bugs.\n• Third-party code: libraries are tested by their maintainers.\n• Trivial getters and setters with no logic.\n• Exact visual design — unless it matters enough for dedicated visual regression tests.\n\nThe question I use as a filter: if this broke, would a user notice? If not, the test probably has no value.\n\nAnd on coverage: 100% is not a goal. High coverage from tests that assert implementation details is worse than 60% covering the important paths well. Coverage reveals what was never tested, but it proves nothing about the quality of what was.',
    },
  },
  {
    id: 'test-jest-basics',
    difficulty: beginner,
    question: {
      ar: 'كيف تكتب اختبارًا باستخدام Jest أو Vitest؟',
      en: 'How do you write a test with Jest or Vitest?',
    },
    answer: {
      ar: '```js\nimport { describe, it, expect, beforeEach, vi } from "vitest";\nimport { calculateTotal } from "./cart";\n\ndescribe("calculateTotal", () => {\n  it("يجمع أسعار العناصر", () => {\n    const items = [{ price: 10, qty: 2 }, { price: 5, qty: 1 }];\n    expect(calculateTotal(items)).toBe(25);\n  });\n\n  it("يعيد صفرًا للسلة الفارغة", () => {\n    expect(calculateTotal([])).toBe(0);\n  });\n\n  it("يطبّق الخصم", () => {\n    expect(calculateTotal([{ price: 100, qty: 1 }], 0.1)).toBe(90);\n  });\n});\n```\n\nبنية الاختبار الجيد تتبع نمط AAA:\n1. Arrange: تجهيز البيانات.\n2. Act: تنفيذ ما تختبره.\n3. Assert: التحقق من النتيجة.\n\nمطابقات شائعة:\n```js\nexpect(value).toBe(5);              // مقارنة مرجعية (Object.is)\nexpect(obj).toEqual({ a: 1 });      // مقارنة بالمحتوى\nexpect(arr).toContain("item");\nexpect(fn).toThrow("رسالة");\nexpect(mock).toHaveBeenCalledWith(1, 2);\nawait expect(promise).resolves.toBe(5);\n```\nفرق مهم: toBe للقيم الأولية، وtoEqual للكائنات والمصفوفات — استخدام toBe مع كائنين متطابقي المحتوى يفشل دائمًا.\n\nالفرق بين الأداتين: Vitest أسرع بكثير ومتوافق مع واجهة Jest، ويتكامل مع Vite مباشرة بلا إعداد. Jest أكثر انتشارًا وله منظومة أكبر. للمشاريع الجديدة المبنية على Vite، Vitest هو الخيار الطبيعي.\n\nنصيحة على التسمية: اجعل اسم الاختبار يصف السلوك المتوقع، لا اسم الدالة. الاسم الجيد يشرح الخطأ دون قراءة الكود.',
      en: '```js\nimport { describe, it, expect, beforeEach, vi } from "vitest";\nimport { calculateTotal } from "./cart";\n\ndescribe("calculateTotal", () => {\n  it("sums the item prices", () => {\n    const items = [{ price: 10, qty: 2 }, { price: 5, qty: 1 }];\n    expect(calculateTotal(items)).toBe(25);\n  });\n\n  it("returns zero for an empty cart", () => {\n    expect(calculateTotal([])).toBe(0);\n  });\n\n  it("applies a discount", () => {\n    expect(calculateTotal([{ price: 100, qty: 1 }], 0.1)).toBe(90);\n  });\n});\n```\n\nA good test follows the AAA structure:\n1. Arrange: set up the data.\n2. Act: run what you are testing.\n3. Assert: check the result.\n\nCommon matchers:\n```js\nexpect(value).toBe(5);              // identity (Object.is)\nexpect(obj).toEqual({ a: 1 });      // structural equality\nexpect(arr).toContain("item");\nexpect(fn).toThrow("message");\nexpect(mock).toHaveBeenCalledWith(1, 2);\nawait expect(promise).resolves.toBe(5);\n```\nAn important distinction: toBe for primitives, toEqual for objects and arrays — using toBe on two structurally identical objects always fails.\n\nThe difference between the tools: Vitest is much faster, API-compatible with Jest, and integrates with Vite with no configuration. Jest is more widespread with a larger ecosystem. For new Vite-based projects, Vitest is the natural choice.\n\nA naming tip: describe the expected behaviour, not the function name. A good test name explains the failure without reading the code.',
    },
  },

  // ------------------------------------------------------------ INTERMEDIATE
  {
    id: 'test-rtl',
    difficulty: intermediate,
    question: {
      ar: 'ما هي React Testing Library وما فلسفتها؟',
      en: 'What is React Testing Library and what is its philosophy?',
    },
    answer: {
      ar: 'فلسفتها في جملة واحدة من توثيقها: "كلما شابهت اختباراتك طريقة استخدام البرنامج، زادت ثقتك بها".\n\nعمليًا: اختبر ما يراه المستخدم ويفعله، لا الحالة الداخلية للمكوّن.\n\n```jsx\nimport { render, screen } from "@testing-library/react";\nimport userEvent from "@testing-library/user-event";\n\nit("يعرض رسالة خطأ عند بريد غير صالح", async () => {\n  const user = userEvent.setup();\n  render(<LoginForm onSubmit={vi.fn()} />);\n\n  await user.type(screen.getByLabelText(/البريد/i), "غير-صالح");\n  await user.click(screen.getByRole("button", { name: /دخول/i }));\n\n  expect(await screen.findByText(/بريد غير صالح/i)).toBeInTheDocument();\n});\n```\n\nترتيب المحدِّدات بحسب الأفضلية — وهو جوهر المكتبة:\n1. getByRole — الأفضل، لأنه يعكس ما تراه التقنيات المساعدة فعلًا.\n2. getByLabelText — لحقول النماذج.\n3. getByText — للمحتوى المرئي.\n4. getByTestId — الملاذ الأخير فقط.\n\nمكسب غير متوقع: استخدام getByRole يكشف مشاكل إمكانية الوصول تلقائيًا. إن لم تستطع العثور على زرك بدوره، فقارئ الشاشة لا يجده أيضًا.\n\nفروق مهمة بين الاستعلامات:\n• getBy: يرمي خطأ إن لم يجد — للعناصر التي يجب أن تكون موجودة.\n• queryBy: يعيد null — للتأكد من عدم وجود عنصر.\n• findBy: غير متزامن وينتظر الظهور — للمحتوى الذي يظهر بعد عملية.\n\nوما لا تفعله المكتبة عمدًا: لا تتيح الوصول إلى الحالة الداخلية أو الـ props. هذا قيد مقصود يمنعك من اختبار التنفيذ.',
      en: 'Its philosophy in one line from its own docs: "the more your tests resemble the way your software is used, the more confidence they can give you".\n\nIn practice: test what the user sees and does, not the component\'s internal state.\n\n```jsx\nimport { render, screen } from "@testing-library/react";\nimport userEvent from "@testing-library/user-event";\n\nit("shows an error for an invalid email", async () => {\n  const user = userEvent.setup();\n  render(<LoginForm onSubmit={vi.fn()} />);\n\n  await user.type(screen.getByLabelText(/email/i), "not-an-email");\n  await user.click(screen.getByRole("button", { name: /sign in/i }));\n\n  expect(await screen.findByText(/invalid email/i)).toBeInTheDocument();\n});\n```\n\nThe query priority order — the heart of the library:\n1. getByRole — best, because it reflects what assistive technology actually sees.\n2. getByLabelText — for form fields.\n3. getByText — for visible content.\n4. getByTestId — a last resort only.\n\nAn unexpected benefit: using getByRole surfaces accessibility problems automatically. If you cannot find your button by its role, a screen reader cannot find it either.\n\nImportant query distinctions:\n• getBy: throws if not found — for elements that must exist.\n• queryBy: returns null — for asserting something is absent.\n• findBy: async and waits for appearance — for content that arrives after an action.\n\nAnd what the library deliberately does not provide: access to internal state or props. That restriction is intentional, to stop you testing implementation.',
    },
  },
  {
    id: 'test-mocking',
    difficulty: intermediate,
    question: {
      ar: 'ما هو الـ Mocking ومتى تستخدمه؟',
      en: 'What is mocking and when should you use it?',
    },
    answer: {
      ar: 'الـ mocking هو استبدال اعتمادية حقيقية بنسخة مضبوطة أثناء الاختبار.\n\n```js\n// دالة وهمية\nconst onSave = vi.fn();\nexpect(onSave).toHaveBeenCalledWith({ id: 1 });\n\n// استبدال وحدة كاملة\nvi.mock("./api", () => ({\n  fetchUser: vi.fn().mockResolvedValue({ id: 1, name: "Sara" }),\n}));\n\n// التحكم في الوقت\nvi.useFakeTimers();\nvi.advanceTimersByTime(5000);\n```\n\nمتى أستخدمه؟\n• استدعاءات شبكة — لتجنّب البطء وعدم الثبات.\n• الوقت والعشوائية — لجعل النتيجة حتمية.\n• واجهات المتصفح غير المتاحة في بيئة الاختبار.\n• عمليات مكلفة أو ذات آثار جانبية حقيقية (إرسال بريد، دفع).\n\nمتى لا أستخدمه؟ عندما يصبح الاختبار مرآة للتنفيذ. إن كان اختبارك يستبدل كل شيء ثم يتحقق من أن الدوال استُدعيت، فهو لا يثبت أن الميزة تعمل.\n\nبديل أفضل لاستدعاءات الشبكة: اعتراضها على مستوى الشبكة بـ MSW (Mock Service Worker) بدل استبدال وحدة الـ API:\n```js\nserver.use(\n  http.get("/api/users/:id", () => HttpResponse.json({ id: 1, name: "Sara" }))\n);\n```\nلماذا أفضل؟ لأن الكود الحقيقي يعمل كاملًا — fetch والتحليل ومعالجة الأخطاء — ويبقى الاختبار صالحًا إن غيّرت مكتبة الطلبات. كما يمكن استخدام نفس التعريفات في التطوير.\n\nوقاعدة مهمة: كل mock هو افتراض عن سلوك النظام الحقيقي. إن تغيّر ذلك السلوك، تمر اختباراتك بينما ينكسر الإنتاج — لهذا لا تُستغنى اختبارات التكامل الحقيقية تمامًا.',
      en: 'Mocking means replacing a real dependency with a controlled stand-in during a test.\n\n```js\n// a mock function\nconst onSave = vi.fn();\nexpect(onSave).toHaveBeenCalledWith({ id: 1 });\n\n// replacing a whole module\nvi.mock("./api", () => ({\n  fetchUser: vi.fn().mockResolvedValue({ id: 1, name: "Sara" }),\n}));\n\n// controlling time\nvi.useFakeTimers();\nvi.advanceTimersByTime(5000);\n```\n\nWhen do I use it?\n• Network calls — to avoid slowness and flakiness.\n• Time and randomness — to make results deterministic.\n• Browser APIs unavailable in the test environment.\n• Expensive operations with real side effects (sending email, taking payment).\n\nWhen do I not? When the test becomes a mirror of the implementation. If your test mocks everything and then asserts which functions were called, it proves nothing about the feature working.\n\nA better alternative for network calls: intercept at the network level with MSW (Mock Service Worker) rather than replacing the API module:\n```js\nserver.use(\n  http.get("/api/users/:id", () => HttpResponse.json({ id: 1, name: "Sara" }))\n);\n```\nWhy better? Because the real code path runs — fetch, parsing, error handling — and the test survives swapping your HTTP client. You can also reuse the same handlers in development.\n\nAn important caveat: every mock encodes an assumption about the real system. If that behaviour changes, your tests pass while production breaks — which is why real integration tests never become entirely redundant.',
    },
  },
  {
    id: 'test-async',
    difficulty: intermediate,
    question: {
      ar: 'كيف تختبر الكود غير المتزامن؟',
      en: 'How do you test asynchronous code?',
    },
    answer: {
      ar: '```js\n// دالة تعيد Promise\nit("يجلب المستخدم", async () => {\n  const user = await fetchUser(1);\n  expect(user.name).toBe("Sara");\n});\n\n// اختبار الرفض\nit("يرمي خطأ لمعرّف غير موجود", async () => {\n  await expect(fetchUser(999)).rejects.toThrow("Not found");\n});\n\n// في المكوّنات — انتظار الظهور\nit("يعرض البيانات بعد التحميل", async () => {\n  render(<UserProfile id={1} />);\n\n  expect(screen.getByText(/جارٍ التحميل/i)).toBeInTheDocument();\n\n  expect(await screen.findByText("Sara")).toBeInTheDocument();\n  expect(screen.queryByText(/جارٍ التحميل/i)).not.toBeInTheDocument();\n});\n\n// انتظار شرط مركّب\nawait waitFor(() => {\n  expect(mockSave).toHaveBeenCalledTimes(1);\n});\n```\n\nالأخطاء الشائعة:\n\n1. نسيان await — الاختبار ينتهي قبل اكتمال العملية فيمر زورًا أو يفشل بشكل غامض.\n\n2. استخدام getBy بدل findBy لمحتوى يظهر لاحقًا — getBy لا ينتظر ويفشل فورًا.\n\n3. انتظار بمدة ثابتة:\n```js\nawait new Promise((r) => setTimeout(r, 1000));   // سيئ\n```\nهذا يبطئ الاختبارات ويظل هشًا — استخدم findBy أو waitFor.\n\n4. تحذير act في React: عادة يعني أن تحديث حالة حدث خارج نطاق انتظارك. استخدام findBy أو waitFor بشكل صحيح يحله؛ ولا تتجاهله لأنه يشير إلى اختبار غير مستقر.\n\n5. المؤقتات الوهمية مع async تحتاج انتباهًا: استخدم vi.useFakeTimers مع advanceTimersByTimeAsync.\n\nونصيحة عن الاختبارات الهشّة (flaky): أي اختبار يفشل أحيانًا يجب إصلاحه لا إعادة تشغيله. السبب غالبًا انتظار غير صحيح أو حالة مشتركة بين الاختبارات.',
      en: '```js\n// a promise-returning function\nit("fetches the user", async () => {\n  const user = await fetchUser(1);\n  expect(user.name).toBe("Sara");\n});\n\n// testing rejection\nit("throws for a missing id", async () => {\n  await expect(fetchUser(999)).rejects.toThrow("Not found");\n});\n\n// in components — waiting for appearance\nit("renders data after loading", async () => {\n  render(<UserProfile id={1} />);\n\n  expect(screen.getByText(/loading/i)).toBeInTheDocument();\n\n  expect(await screen.findByText("Sara")).toBeInTheDocument();\n  expect(screen.queryByText(/loading/i)).not.toBeInTheDocument();\n});\n\n// waiting for a compound condition\nawait waitFor(() => {\n  expect(mockSave).toHaveBeenCalledTimes(1);\n});\n```\n\nThe common mistakes:\n\n1. Forgetting await — the test finishes before the operation does, so it passes falsely or fails mysteriously.\n\n2. Using getBy instead of findBy for content that appears later — getBy does not wait and fails immediately.\n\n3. Waiting a fixed duration:\n```js\nawait new Promise((r) => setTimeout(r, 1000));   // bad\n```\nThis slows the suite and stays flaky — use findBy or waitFor.\n\n4. React\'s act warning: it usually means a state update happened outside anything you awaited. Using findBy or waitFor correctly resolves it, and you should not suppress it, because it signals an unstable test.\n\n5. Fake timers with async code need care: pair vi.useFakeTimers with advanceTimersByTimeAsync.\n\nAnd a note on flaky tests: a test that fails intermittently must be fixed, not re-run. The cause is almost always an incorrect wait or state shared between tests.',
    },
  },
  {
    id: 'test-strategy',
    difficulty: intermediate,
    question: {
      ar: 'كيف تضع استراتيجية اختبار لمشروع؟',
      en: 'How do you define a testing strategy for a project?',
    },
    answer: {
      ar: 'أبني الاستراتيجية على سؤال واحد: ما الذي يجب ألا ينكسر أبدًا؟ وأوزّع الجهد وفقًا لذلك.\n\nالتوزيع الذي أعتمده في تطبيق واجهة أمامية:\n\n1. اختبارات وحدات لمنطق العمل النقي: الحسابات، التحويلات، قواعد التحقق. سريعة ورخيصة وعالية القيمة.\n\n2. اختبارات تكامل للمكوّنات والميزات — النصيب الأكبر: مكوّن مع الـ context وطلبات معترضة بـ MSW. هنا تحصل على معظم الثقة.\n\n3. اختبارات E2E للمسارات الحرجة فقط: تسجيل الدخول، الشراء، إنشاء المحتوى. عشرة اختبارات جيدة أفضل من مئة هشّة.\n\n4. اختبارات API/عقود إن كان الخادم منفصلًا.\n\nما أضيفه حسب المشروع:\n• اختبارات لقطات بصرية إن كان التصميم جزءًا من المنتج.\n• اختبارات إمكانية وصول آلية (axe) في CI.\n• اختبارات أداء وميزانية حجم الحزم.\n\nقرارات عملية مهمة:\n• سرعة التغذية الراجعة: مجموعة سريعة تعمل مع كل حفظ، ومجموعة أبطأ (E2E) في CI فقط.\n• الاستقلالية: كل اختبار ينشئ بياناته وينظّفها؛ لا اعتماد على ترتيب التنفيذ.\n• التعامل مع الهشاشة: اختبار هشّ يجب إصلاحه أو حذفه، لأن مجموعة تفشل عشوائيًا يتوقف الفريق عن قراءتها وتفقد كل قيمتها.\n• كل خطأ إنتاج يضيف اختبارًا.\n\nوالمعيار النهائي: هل يستطيع مطور جديد نشر تغيير وهو واثق؟ إن كان الفريق يعتمد على اختبار يدوي قبل كل نشر، فالاستراتيجية لم تنجح بعد.',
      en: 'I build the strategy around one question: what must never break? And I allocate effort accordingly.\n\nThe distribution I use on a frontend application:\n\n1. Unit tests for pure business logic: calculations, transformations, validation rules. Fast, cheap and high value.\n\n2. Integration tests for components and features — the largest share: a component with its context and requests intercepted by MSW. This is where most of the confidence comes from.\n\n3. E2E tests for critical paths only: login, checkout, content creation. Ten good ones beat a hundred flaky ones.\n\n4. API or contract tests when the backend is a separate service.\n\nWhat I add depending on the project:\n• Visual regression tests when design is part of the product.\n• Automated accessibility checks (axe) in CI.\n• Performance tests and a bundle size budget.\n\nImportant practical decisions:\n• Feedback speed: a fast suite that runs on save, and a slower one (E2E) in CI only.\n• Independence: every test creates and cleans up its own data; never depend on execution order.\n• Flakiness policy: a flaky test must be fixed or deleted, because a suite that fails randomly is a suite the team stops reading, and it loses all its value.\n• Every production bug adds a test.\n\nThe ultimate measure: can a new developer ship a change with confidence? If the team still relies on manual testing before every release, the strategy has not landed yet.',
    },
  },
  {
    id: 'test-common-mistakes',
    difficulty: intermediate,
    question: {
      ar: 'ما هي الأخطاء الشائعة في كتابة الاختبارات؟',
      en: 'What are the common mistakes when writing tests?',
    },
    answer: {
      ar: 'الأخطاء التي أراها أكثر من غيرها:\n\n1. اختبار التنفيذ لا السلوك: التحقق من حالة داخلية أو من استدعاء دالة داخلية. النتيجة اختبارات تنكسر عند كل إعادة هيكلة دون كشف خطأ واحد — فيبدأ الفريق بكراهية الاختبارات.\n\n2. الإفراط في الـ mocking حتى لا يبقى شيء حقيقي يُختبر.\n\n3. اختبارات مترابطة: اختبار يعتمد على بيانات أنشأها آخر. تفشل عند تغيير الترتيب أو التنفيذ المتوازي.\n\n4. حالة مشتركة لم تُنظَّف: قاعدة بيانات، أو localStorage، أو mocks بلا إعادة ضبط. استخدم beforeEach وafterEach بانضباط.\n\n5. انتظار بمدة ثابتة بدل انتظار شرط — بطيء وهشّ معًا.\n\n6. تأكيدات كثيرة في اختبار واحد: عند الفشل لا تعرف أي سلوك انكسر فعلًا.\n\n7. أسماء غامضة مثل "test 1" أو "it works". الاسم الجيد يشرح الفشل دون فتح الكود.\n\n8. مطاردة نسبة تغطية 100%: تنتج اختبارات لا قيمة لها لكود بسيط، وتعطي إحساسًا زائفًا بالأمان.\n\n9. تجاهل الاختبارات الهشّة أو تعطيلها بـ skip — الدَّين يتراكم حتى تصبح المجموعة بلا معنى.\n\n10. عدم اختبار حالات الفشل: معظم الاختبارات تغطي المسار السعيد فقط، بينما الأخطاء تعيش في الحالات الأخرى.\n\n11. اختبار لا يفشل أبدًا: تأكد أن اختبارك يفشل عند كسر الكود عمدًا — اختبار لا يفشل ليس اختبارًا.\n\nوالخيط الجامع: الاختبار الجيد يعطيك ثقة عند التغيير. إن كان يمنعك من التغيير فهو عبء لا أصل.',
      en: 'The ones I see most often:\n\n1. Testing implementation instead of behaviour: asserting internal state or that an internal function was called. The result is tests that break on every refactor without catching a single bug — and the team starts resenting tests.\n\n2. Mocking so heavily that nothing real is left under test.\n\n3. Coupled tests: one depending on data another created. They fail when the order changes or under parallel execution.\n\n4. Shared state that is never cleaned: a database, localStorage, mocks that are not reset. Use beforeEach and afterEach with discipline.\n\n5. Waiting a fixed duration instead of waiting for a condition — slow and flaky at the same time.\n\n6. Too many assertions in one test: when it fails you cannot tell which behaviour actually broke.\n\n7. Vague names such as "test 1" or "it works". A good name explains the failure without opening the code.\n\n8. Chasing 100% coverage: it produces worthless tests for trivial code and a false sense of safety.\n\n9. Ignoring or skipping flaky tests — the debt accumulates until the suite means nothing.\n\n10. Not testing failure paths: most suites cover only the happy path, while bugs live everywhere else.\n\n11. A test that can never fail: check that your test fails when you deliberately break the code. A test that cannot fail is not a test.\n\nThe common thread: a good test gives you confidence to change things. If it stops you changing things, it is a liability rather than an asset.',
    },
  },

  // ---------------------------------------------------------------- ADVANCED
  {
    id: 'test-e2e-advanced',
    difficulty: advanced,
    question: {
      ar: 'كيف تكتب اختبارات E2E مستقرة وغير هشّة؟',
      en: 'How do you write stable, non-flaky E2E tests?',
    },
    answer: {
      ar: 'الهشاشة هي السبب الأول لفقدان الثقة في اختبارات E2E. ما أطبّقه لتجنّبها:\n\n1. محدِّدات مستقرة: استخدم الأدوار وأسماء العناصر المرئية أو data-testid مخصص، ولا تستخدم مسارات CSS هشّة مثل `div > div:nth-child(3)` — تنكسر عند أي تعديل في التصميم.\n\n2. انتظار صحيح: الأدوات الحديثة مثل Playwright تنتظر تلقائيًا حتى يصبح العنصر قابلًا للتفاعل. لا تستخدم انتظارًا بمدة ثابتة إطلاقًا.\n```js\nawait expect(page.getByRole("heading", { name: "لوحة التحكم" })).toBeVisible();\n```\n\n3. بيانات معزولة: كل اختبار ينشئ مستخدمه وبياناته. اختبارات تتشارك حسابًا واحدًا تتعارض فور التشغيل المتوازي.\n\n4. تجاوز الواجهة للإعداد: لا تسجّل الدخول عبر النموذج في كل اختبار. سجّل مرة واحدة واحفظ حالة الجلسة، ثم أعد استخدامها. هذا يوفّر وقتًا هائلًا ويقلّل نقاط الفشل.\n\n5. تحكّم في الخدمات الخارجية: اعترض استدعاءات الطرف الثالث — بوابة الدفع مثلًا — لأن اعتماد اختباراتك على خدمة خارجية يجعلها تفشل لأسباب خارجة عن سيطرتك.\n\n6. بيئة ثابتة: منطقة زمنية ولغة وحجم نافذة محددة، وإلا اختلفت النتائج بين جهاز المطوّر وCI.\n\n7. التشخيص عند الفشل: فعّل التقاط لقطة شاشة وفيديو وtrace. اختبار CI يفشل بلا دليل مضيعة للوقت.\n\n8. النطاق: اختبر المسارات الحرجة فقط. مئة اختبار E2E تستغرق ساعة وتفشل عشوائيًا أسوأ من عشرين اختبارًا موثوقًا.\n\n9. سياسة صارمة: اختبار هشّ يُصلَح خلال يومين أو يُحذف. إعادة التشغيل حتى ينجح عادة قاتلة.',
      en: 'Flakiness is the main reason teams lose trust in E2E tests. What I do to avoid it:\n\n1. Stable selectors: use roles and visible names, or a dedicated data-testid. Never brittle CSS paths like `div > div:nth-child(3)` — they break on any design tweak.\n\n2. Correct waiting: modern tools such as Playwright auto-wait until an element is actionable. Never use fixed sleeps.\n```js\nawait expect(page.getByRole("heading", { name: "Dashboard" })).toBeVisible();\n```\n\n3. Isolated data: each test creates its own user and records. Tests sharing one account collide the moment you run them in parallel.\n\n4. Bypass the UI for setup: do not log in through the form in every test. Authenticate once, save the storage state, and reuse it. This saves enormous time and removes failure points.\n\n5. Control external services: intercept third-party calls such as a payment gateway, because depending on an external service makes your tests fail for reasons outside your control.\n\n6. A fixed environment: pinned timezone, locale and viewport, otherwise results differ between a developer machine and CI.\n\n7. Diagnostics on failure: enable screenshots, video and traces. A CI failure with no evidence is a waste of everyone\'s time.\n\n8. Scope: test critical paths only. A hundred E2E tests taking an hour and failing randomly are worse than twenty reliable ones.\n\n9. A strict policy: a flaky test is fixed within two days or deleted. Re-running until green is a fatal habit.',
    },
  },
  {
    id: 'test-scenario-legacy',
    difficulty: advanced,
    question: {
      ar: 'سيناريو: انضممت إلى مشروع بلا اختبارات إطلاقًا. كيف تبدأ؟',
      en: 'Scenario: you join a project with no tests at all. How do you start?',
    },
    answer: {
      ar: 'لا أحاول تغطية كل شيء — هذا يفشل دائمًا. أبدأ حيث تكون القيمة أعلى والتكلفة أقل.\n\nالخطوات:\n\n1. البنية التحتية أولًا: أعدّ أداة الاختبار وأشغّلها في CI، ولو باختبار واحد. الحاجز الأول نفسي وتقني معًا.\n\n2. ابدأ بالمسارات الحرجة عبر E2E: ثلاثة إلى خمسة اختبارات تغطي أهم رحلات المستخدم. لماذا E2E أولًا رغم بطئها؟ لأنها لا تتطلب تغيير الكود القائم إطلاقًا، وتعطي شبكة أمان فورية تسمح بإعادة الهيكلة لاحقًا بثقة.\n\n3. اختبار عند كل إصلاح خطأ: كل خطأ إنتاج يُكتب له اختبار يعيد إنتاجه قبل الإصلاح. هذا يبني التغطية تدريجيًا حيث توجد الأخطاء فعلًا — وهو أذكى توزيع ممكن للجهد.\n\n4. اختبار عند كل كود جديد: لا كود جديد بلا اختبار. هذا يمنع تفاقم الدَّين.\n\n5. استخرج المنطق تدريجيًا: الكود القديم غالبًا يخلط منطق العمل بالواجهة. عند تعديل أي جزء، استخرج المنطق إلى دالة نقية واختبرها. هذا يحسّن البنية والتغطية معًا.\n\n6. حدّد الأجزاء عالية المخاطر: ما يتغير كثيرًا، وما ينكسر كثيرًا، وما يمسّ المال أو البيانات. ابدأ بها.\n\nما أتجنّبه:\n• مشروع "إضافة اختبارات" منفصل عن العمل — يُلغى عند أول ضغط زمني.\n• هدف تغطية رقمي مفروض — ينتج اختبارات شكلية.\n• إعادة كتابة الكود لجعله قابلًا للاختبار قبل وجود أي شبكة أمان — هذا أخطر ترتيب ممكن.\n\nوالتوقع الواقعي: بناء ثقافة اختبار في مشروع قائم يستغرق أشهرًا، ويتم عبر التراكم لا عبر حملة واحدة.',
      en: 'I do not try to cover everything — that always fails. I start where value is highest and cost is lowest.\n\nThe steps:\n\n1. Infrastructure first: set up the test runner and wire it into CI, even with one test. The first barrier is as much psychological as technical.\n\n2. Start with critical paths via E2E: three to five tests covering the most important user journeys. Why E2E first despite the cost? Because they require no changes to existing code and give you an immediate safety net that makes later refactoring possible.\n\n3. A test with every bug fix: every production bug gets a reproducing test before the fix. This grows coverage exactly where bugs actually are — the smartest possible allocation of effort.\n\n4. A test with every new piece of code: no new code without tests. That stops the debt growing.\n\n5. Extract logic incrementally: legacy code usually mixes business logic with UI. Whenever you touch a piece, extract the logic into a pure function and test it. That improves structure and coverage together.\n\n6. Identify high-risk areas: what changes often, what breaks often, and what touches money or data. Start there.\n\nWhat I avoid:\n• A separate "add tests" project detached from feature work — it gets cancelled at the first deadline.\n• An imposed numeric coverage target — it produces box-ticking tests.\n• Rewriting code for testability before any safety net exists — the most dangerous possible order.\n\nA realistic expectation: building a testing culture in an existing project takes months, and it happens by accumulation rather than by campaign.',
    },
  },
];
