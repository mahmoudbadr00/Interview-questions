// data/categories/stateManagement.js
import { DIFFICULTY } from '../difficulty.js';

const { beginner, intermediate, advanced } = DIFFICULTY;

export const stateManagement = [
  // ---------------------------------------------------------------- BEGINNER
  {
    id: 'state-local-vs-global',
    difficulty: beginner,
    question: {
      ar: 'ما الفرق بين الحالة المحلية والحالة العامة؟ ومتى تحتاج حالة عامة؟',
      en: 'What is the difference between local and global state, and when do you actually need global state?',
    },
    answer: {
      ar: 'الحالة المحلية يملكها مكوّن واحد ولا يعرفها غيره: فتح قائمة، قيمة حقل، تبويب نشط.\nالحالة العامة تحتاجها أجزاء متباعدة من التطبيق: المستخدم الحالي، السمة، سلة الشراء.\n\nالقاعدة التي أبدأ منها: كل حالة محلية حتى يثبت العكس.\n\nمتى أحتاج حالة عامة فعلًا؟ عند توفر شرطين معًا:\n1. تحتاجها مكوّنات في فروع مختلفة من الشجرة.\n2. لا يمكن حلها بالتركيب أو برفع الحالة درجة أو درجتين.\n\nالخطأ الأشهر: نقل كل شيء إلى مخزن عام لأنه "أسهل". النتائج:\n• عمليات إعادة تصيير غير ضرورية في أنحاء التطبيق.\n• صعوبة معرفة من يغيّر ماذا.\n• مكوّنات لا يمكن إعادة استخدامها أو اختبارها بمعزل.\n• كود إضافي كثير لحالة لا يحتاجها أحد غير مكوّن واحد.\n\nوالسؤال الأهم قبل أي مخزن عام: هل هذه بيانات خادم أصلًا؟ قائمة المنتجات وملف المستخدم ونتائج البحث ليست حالة تطبيق بل نسخة مؤقتة من بيانات موجودة على الخادم، ومكانها الصحيح مكتبة مثل React Query.\n\nبعد إزالة بيانات الخادم من المعادلة، تكتشف أن الحالة العامة الحقيقية في معظم التطبيقات قليلة جدًا: المصادقة، والتفضيلات، وربما سلة أو إشعارات.',
      en: 'Local state is owned by one component and unknown to anything else: a menu being open, an input value, the active tab.\nGlobal state is needed by distant parts of the application: the current user, the theme, the shopping cart.\n\nThe rule I start from: all state is local until proven otherwise.\n\nWhen do I genuinely need global state? When two conditions hold together:\n1. Components in different branches of the tree need it.\n2. It cannot be solved with composition or by lifting state one or two levels.\n\nThe classic mistake is moving everything into a global store because it is "easier". The consequences:\n• Unnecessary re-renders across the app.\n• Difficulty knowing who changes what.\n• Components that cannot be reused or tested in isolation.\n• A lot of boilerplate for state only one component needs.\n\nAnd the most important question before reaching for any store: is this actually server data? A product list, a user profile, search results — none of those are application state. They are a cache of data that lives on the server, and they belong in something like React Query.\n\nOnce you take server data out of the equation, you find that genuine global state in most applications is tiny: authentication, preferences, and perhaps a cart or a notification queue.',
    },
  },
  {
    id: 'state-context-basics',
    difficulty: beginner,
    question: {
      ar: 'كيف تستخدم Context API لإدارة الحالة؟',
      en: 'How do you use the Context API for state management?',
    },
    answer: {
      ar: '```jsx\nconst AuthContext = createContext(null);\n\nexport function AuthProvider({ children }) {\n  const [user, setUser] = useState(null);\n\n  const login = useCallback(async (credentials) => {\n    setUser(await api.login(credentials));\n  }, []);\n\n  const logout = useCallback(() => setUser(null), []);\n\n  const value = useMemo(() => ({ user, login, logout }), [user, login, logout]);\n\n  return <AuthContext.Provider value={value}>{children}</AuthContext.Provider>;\n}\n\nexport function useAuth() {\n  const context = useContext(AuthContext);\n  if (!context) throw new Error("useAuth يجب أن يُستخدم داخل AuthProvider");\n  return context;\n}\n```\n\nنقاط مهمة في هذا النمط:\n1. useMemo على قيمة الـ Provider ضروري: بدونه يُنشأ كائن جديد كل تصيير، فيُعاد تصيير كل المستهلكين حتى لو لم يتغير شيء.\n2. تغليف useContext في hook مخصص يعطي رسالة خطأ واضحة عند الاستخدام خارج الـ Provider، ويخفي تفاصيل الـ context.\n3. useCallback للدوال حتى لا تتغير مراجعها.\n\nما يجب معرفته عن حدود Context: أي تغيير في القيمة يعيد تصيير كل مستهلك، حتى من يهتم بجزء لم يتغير. لذلك هو مناسب للقيم قليلة التغيّر (المصادقة، السمة، اللغة) وغير مناسب للحالة سريعة التغيّر.\n\nوتصحيح لمفهوم شائع: Context ليس أداة إدارة حالة، بل آلية لتمرير قيمة عبر الشجرة. الحالة نفسها ما زالت في useState أو useReducer؛ Context يوزّعها فقط.',
      en: '```jsx\nconst AuthContext = createContext(null);\n\nexport function AuthProvider({ children }) {\n  const [user, setUser] = useState(null);\n\n  const login = useCallback(async (credentials) => {\n    setUser(await api.login(credentials));\n  }, []);\n\n  const logout = useCallback(() => setUser(null), []);\n\n  const value = useMemo(() => ({ user, login, logout }), [user, login, logout]);\n\n  return <AuthContext.Provider value={value}>{children}</AuthContext.Provider>;\n}\n\nexport function useAuth() {\n  const context = useContext(AuthContext);\n  if (!context) throw new Error("useAuth must be used within AuthProvider");\n  return context;\n}\n```\n\nWhat matters in this pattern:\n1. The useMemo on the provider value is essential: without it a new object is created every render, re-rendering every consumer even when nothing changed.\n2. Wrapping useContext in a custom hook gives a clear error when used outside the provider, and hides the context details.\n3. useCallback on the functions so their references stay stable.\n\nThe limitation to understand: any change to the value re-renders every consumer, including ones that only care about a part that did not change. So context suits slow-changing values (auth, theme, locale) and does not suit rapidly changing state.\n\nAnd a correction to a common misconception: Context is not a state manager. It is a mechanism for passing a value down the tree. The state itself still lives in useState or useReducer; context only distributes it.',
    },
  },
  {
    id: 'state-redux-what',
    difficulty: beginner,
    question: {
      ar: 'ما هو Redux وكيف يعمل؟',
      en: 'What is Redux and how does it work?',
    },
    answer: {
      ar: 'Redux مكتبة لإدارة حالة التطبيق في مخزن واحد مركزي، بتدفق بيانات أحادي الاتجاه صارم.\n\nمبادئه الثلاثة:\n1. مصدر واحد للحقيقة: كل الحالة في كائن واحد.\n2. الحالة للقراءة فقط: لا تُعدَّل مباشرة، بل بإرسال action.\n3. التغييرات عبر دوال نقية: الـ reducer يأخذ الحالة الحالية والـ action ويعيد حالة جديدة.\n\nالتدفق: مكوّن يُرسل action ← middleware ← reducer ينتج حالة جديدة ← المكوّنات المشتركة تُحدَّث.\n\n```js\n// النمط الحديث مع Redux Toolkit\nconst cartSlice = createSlice({\n  name: "cart",\n  initialState: { items: [] },\n  reducers: {\n    addItem(state, action) {\n      state.items.push(action.payload);   // يبدو تعديلًا لكنه ليس كذلك\n    },\n    removeItem(state, action) {\n      state.items = state.items.filter((i) => i.id !== action.payload);\n    },\n  },\n});\n\nexport const { addItem, removeItem } = cartSlice.actions;\n```\n\nملاحظة مهمة على المثال: الكود يبدو وكأنه يعدّل الحالة مباشرة، لكن Redux Toolkit يستخدم Immer داخليًا، فيحوّل هذا إلى تحديث غير معدِّل (immutable) خلف الكواليس. هذا أزال أكثر ما كان يُنتقد في Redux القديم.\n\nما يقدّمه Redux فعليًا: تتبّع واضح لكل تغيير، وأدوات تطوير ممتازة (time-travel debugging)، وسلوك متوقع في التطبيقات المعقّدة.\n\nوالتوصية الرسمية اليوم: استخدم Redux Toolkit دائمًا. Redux التقليدي بكل الـ boilerplate لم يعد موصى به.',
      en: 'Redux is a library for managing application state in a single central store, with a strictly one-way data flow.\n\nIts three principles:\n1. A single source of truth: all state in one object.\n2. State is read-only: you never mutate it, you dispatch an action.\n3. Changes happen through pure functions: a reducer takes the current state and an action and returns new state.\n\nThe flow: a component dispatches an action → middleware → the reducer produces new state → subscribed components update.\n\n```js\n// the modern pattern with Redux Toolkit\nconst cartSlice = createSlice({\n  name: "cart",\n  initialState: { items: [] },\n  reducers: {\n    addItem(state, action) {\n      state.items.push(action.payload);   // looks like mutation, but is not\n    },\n    removeItem(state, action) {\n      state.items = state.items.filter((i) => i.id !== action.payload);\n    },\n  },\n});\n\nexport const { addItem, removeItem } = cartSlice.actions;\n```\n\nAn important note on that example: the code looks like direct mutation, but Redux Toolkit uses Immer internally and turns it into an immutable update behind the scenes. That removed the single biggest complaint about classic Redux.\n\nWhat Redux actually gives you: a clear trace of every change, excellent devtools (time-travel debugging), and predictable behaviour in complex applications.\n\nAnd the official recommendation today: always use Redux Toolkit. Classic Redux with all its boilerplate is no longer recommended.',
    },
  },

  // ------------------------------------------------------------ INTERMEDIATE
  {
    id: 'state-redux-vs-context',
    difficulty: intermediate,
    question: {
      ar: 'ما الفرق بين Redux و Context API؟ ومتى تختار كلًا منهما؟',
      en: 'What is the difference between Redux and the Context API, and when do you choose each?',
    },
    answer: {
      ar: 'التمييز الأول والأهم: هما ليسا بديلين لبعضهما. Context آلية لتمرير قيمة عبر الشجرة، وRedux نظام كامل لإدارة الحالة. في الواقع react-redux يستخدم Context داخليًا.\n\nالفروق العملية:\n\n1. إعادة التصيير: أي تغيير في قيمة Context يعيد تصيير كل المستهلكين. أما Redux فيسمح بالاشتراك الانتقائي عبر selector، فلا يُعاد تصيير المكوّن إلا إذا تغيّر الجزء الذي يقرأه فعلًا. هذا أهم فرق تقني.\n\n2. الأدوات: Redux DevTools تعرض كل action والحالة قبله وبعده، وتسمح بالتنقل بين الحالات. Context لا يوفّر شيئًا مشابهًا.\n\n3. Middleware: Redux يوفّر نقطة تدخّل موحّدة للتسجيل والتتبّع والمنطق غير المتزامن.\n\n4. البنية: Redux يفرض نمطًا واضحًا، وهو مفيد في فرق كبيرة ومبالغة في مشروع صغير.\n\nمتى Context؟ لقيم قليلة التغيّر: المصادقة، السمة، اللغة، الصلاحيات.\n\nمتى Redux؟ عند حالة معقّدة كثيرة التغيّر، أو منطق انتقالات متشابك، أو حاجة حقيقية لتتبّع التغييرات، أو فريق كبير يحتاج نمطًا موحّدًا.\n\nوالخيار الثالث الذي أفضّله في كثير من الحالات: Zustand. يعطي الاشتراك الانتقائي مثل Redux بكود أقل بكثير وبلا boilerplate.\n\nلكن السؤال الأول يبقى: هل هذه بيانات خادم؟ إن كانت كذلك فلا Redux ولا Context — بل React Query.',
      en: 'The first and most important distinction: they are not alternatives to each other. Context is a mechanism for passing a value down the tree; Redux is a complete state management system. In fact react-redux uses Context internally.\n\nThe practical differences:\n\n1. Re-rendering: any change to a context value re-renders every consumer. Redux allows selective subscription via selectors, so a component only re-renders when the slice it actually reads changes. This is the most important technical difference.\n\n2. Tooling: Redux DevTools show every action with the state before and after, and let you step between states. Context offers nothing comparable.\n\n3. Middleware: Redux gives a single interception point for logging, tracing and async logic.\n\n4. Structure: Redux enforces a clear pattern — valuable on a large team, overkill on a small project.\n\nWhen Context? For slow-changing values: auth, theme, locale, permissions.\n\nWhen Redux? For complex, frequently changing state, tangled transition logic, a genuine need to trace changes, or a large team needing a consistent pattern.\n\nAnd the third option I often prefer: Zustand. It gives selective subscription like Redux with far less code and no boilerplate.\n\nBut the first question remains: is this server data? If so, neither Redux nor Context — React Query.',
    },
  },
  {
    id: 'state-server-vs-client',
    difficulty: intermediate,
    question: {
      ar: 'ما الفرق بين حالة الخادم وحالة العميل؟ ولماذا هذا التمييز مهم؟',
      en: 'What is the difference between server state and client state, and why does the distinction matter?',
    },
    answer: {
      ar: 'هذا أهم تمييز في إدارة الحالة في تطبيقات React الحديثة، وإغفاله سبب معظم التعقيد.\n\nحالة العميل: يملكها التطبيق بالكامل — نافذة مفتوحة، تبويب نشط، قيمة حقل، تفضيلات. متزامنة، ولا تحتاج تحديثًا من الخارج.\n\nحالة الخادم: مصدرها الحقيقي قاعدة البيانات، وما لديك مجرد نسخة مؤقتة. خصائصها مختلفة تمامًا:\n• غير متزامنة.\n• قد تتقادم في أي لحظة لأن مستخدمًا آخر عدّلها.\n• مشتركة بين عدة أجزاء.\n• تحتاج تخزينًا مؤقتًا وإعادة جلب وإبطالًا.\n• تحتاج حالات تحميل وخطأ.\n\nلماذا المعاملة الموحّدة خطأ؟ لأن وضع بيانات الخادم في Redux أو useState يجبرك على كتابة يدوية لكل ما سبق: تخزين مؤقت، وإبطال، وإلغاء طلبات قديمة، ومنع طلبات مكررة، وإعادة محاولة. هذا آلاف الأسطر من منطق متكرر ومعرّض للأخطاء.\n\nالحل: أداة مخصصة لحالة الخادم:\n```jsx\nconst { data, isPending, error } = useQuery({\n  queryKey: ["products", filters],\n  queryFn: () => fetchProducts(filters),\n  staleTime: 60_000,\n});\n```\nسطران يغطيان التخزين المؤقت وإعادة الجلب عند العودة للنافذة وإزالة التكرار وإدارة الحالات.\n\nالنتيجة العملية: بعد نقل بيانات الخادم إلى React Query، تكتشف أن ما تبقى في Redux قليل جدًا — وكثير من الفرق تستغني عنه تمامًا.\n\nوفي Next.js مع App Router يذهب هذا أبعد: الجلب في Server Components يعني أن كثيرًا من حالة الخادم لا تصل إلى العميل أصلًا.',
      en: 'This is the most important distinction in modern React state management, and missing it causes most of the complexity.\n\nClient state: fully owned by the application — an open modal, the active tab, an input value, preferences. Synchronous, and never updated from outside.\n\nServer state: its real source is the database, and what you hold is only a cache. Its properties are completely different:\n• Asynchronous.\n• Can go stale at any moment because another user changed it.\n• Shared across several parts of the app.\n• Needs caching, refetching and invalidation.\n• Needs loading and error states.\n\nWhy is treating them the same a mistake? Because putting server data in Redux or useState forces you to hand-write all of the above: caching, invalidation, cancelling stale requests, deduplication, retries. That is thousands of lines of repetitive, error-prone logic.\n\nThe answer is a purpose-built server-state library:\n```jsx\nconst { data, isPending, error } = useQuery({\n  queryKey: ["products", filters],\n  queryFn: () => fetchProducts(filters),\n  staleTime: 60_000,\n});\n```\nTwo lines covering caching, refetch on window focus, deduplication and state management.\n\nThe practical result: once server data moves to React Query, what remains in Redux is very small — and many teams drop it entirely.\n\nAnd in Next.js with the App Router this goes further: fetching in Server Components means much of that server state never reaches the client at all.',
    },
  },
  {
    id: 'state-redux-toolkit',
    difficulty: intermediate,
    question: {
      ar: 'ما هو Redux Toolkit وما الذي يحلّه؟',
      en: 'What is Redux Toolkit and what problems does it solve?',
    },
    answer: {
      ar: 'Redux Toolkit هو الطريقة الرسمية الموصى بها لكتابة Redux، وقد وُجد لحل الشكاوى المشروعة من Redux التقليدي: الكود المكرر الكثير، والإعداد المعقّد، والحاجة لمكتبات إضافية لكل شيء.\n\nما كان مطلوبًا قبله لإضافة ميزة واحدة: ملف لثوابت الـ actions، وملف لمنشئي الـ actions، وملف للـ reducer، وإعداد يدوي للـ store وللـ middleware.\n\nما يقدّمه:\n\n```js\nimport { createSlice, configureStore } from "@reduxjs/toolkit";\n\nconst todosSlice = createSlice({\n  name: "todos",\n  initialState: { items: [], filter: "all" },\n  reducers: {\n    added(state, action) { state.items.push(action.payload); },\n    toggled(state, action) {\n      const todo = state.items.find((t) => t.id === action.payload);\n      if (todo) todo.done = !todo.done;\n    },\n    filterChanged(state, action) { state.filter = action.payload; },\n  },\n});\n\nexport const { added, toggled, filterChanged } = todosSlice.actions;\n\nexport const store = configureStore({\n  reducer: { todos: todosSlice.reducer },\n});\n```\n\nما تغيّر:\n1. createSlice يولّد الـ actions والـ reducer معًا من تعريف واحد.\n2. Immer مدمج: تكتب كودًا يبدو معدِّلًا وتحصل على تحديث غير معدِّل — وهذا أزال أكبر مصدر للأخطاء في Redux القديم (التعديل المباشر بالخطأ).\n3. configureStore يضبط الـ middleware والـ DevTools تلقائيًا، ويضيف فحوصًا تحذّرك من التعديل المباشر ومن القيم غير القابلة للتسلسل.\n4. createAsyncThunk للعمليات غير المتزامنة بحالاتها الثلاث.\n5. RTK Query مدمج لجلب البيانات — بديل عن React Query داخل منظومة Redux.\n\nالنتيجة: كود Redux أقصر بنحو الثلثين وأقل عرضة للأخطاء. ولا يوجد سبب اليوم لكتابة Redux بالطريقة القديمة.',
      en: 'Redux Toolkit is the officially recommended way to write Redux, created to address the legitimate complaints about classic Redux: too much boilerplate, complicated setup, and needing an extra library for everything.\n\nWhat adding one feature used to require: a file of action constants, a file of action creators, a reducer file, and manual store and middleware configuration.\n\nWhat it gives you:\n\n```js\nimport { createSlice, configureStore } from "@reduxjs/toolkit";\n\nconst todosSlice = createSlice({\n  name: "todos",\n  initialState: { items: [], filter: "all" },\n  reducers: {\n    added(state, action) { state.items.push(action.payload); },\n    toggled(state, action) {\n      const todo = state.items.find((t) => t.id === action.payload);\n      if (todo) todo.done = !todo.done;\n    },\n    filterChanged(state, action) { state.filter = action.payload; },\n  },\n});\n\nexport const { added, toggled, filterChanged } = todosSlice.actions;\n\nexport const store = configureStore({\n  reducer: { todos: todosSlice.reducer },\n});\n```\n\nWhat changed:\n1. createSlice generates the actions and the reducer together from one definition.\n2. Immer is built in: you write code that looks mutating and get an immutable update — which removed the biggest source of bugs in classic Redux, accidental direct mutation.\n3. configureStore wires up middleware and DevTools automatically, and adds checks that warn you about mutation and non-serialisable values.\n4. createAsyncThunk for async operations with their three states.\n5. RTK Query is included for data fetching — the Redux ecosystem\'s answer to React Query.\n\nThe result: roughly two-thirds less Redux code and far fewer mistakes. There is no reason today to write Redux the old way.',
    },
  },
  {
    id: 'state-middleware',
    difficulty: intermediate,
    question: {
      ar: 'ما هو الـ Middleware في Redux وكيف تتعامل مع العمليات غير المتزامنة؟',
      en: 'What is middleware in Redux, and how do you handle async operations?',
    },
    answer: {
      ar: 'الـ middleware طبقة تقع بين إرسال الـ action ووصوله إلى الـ reducer، ويمكنها اعتراضه أو تعديله أو تأخيره أو منعه.\n\n```js\nconst logger = (store) => (next) => (action) => {\n  console.group(action.type);\n  console.log("قبل:", store.getState());\n  const result = next(action);\n  console.log("بعد:", store.getState());\n  console.groupEnd();\n  return result;\n};\n```\n\nلماذا نحتاجه للعمليات غير المتزامنة؟ لأن الـ reducer يجب أن يكون دالة نقية — لا استدعاءات شبكة ولا آثار جانبية. الـ middleware هو المكان المخصص لذلك.\n\nمع Redux Toolkit نستخدم createAsyncThunk:\n```js\nexport const fetchUser = createAsyncThunk(\n  "users/fetch",\n  async (userId, { rejectWithValue }) => {\n    try {\n      return await api.getUser(userId);\n    } catch (error) {\n      return rejectWithValue(error.message);\n    }\n  }\n);\n\nconst usersSlice = createSlice({\n  name: "users",\n  initialState: { data: null, status: "idle", error: null },\n  reducers: {},\n  extraReducers: (builder) => {\n    builder\n      .addCase(fetchUser.pending,   (s) => { s.status = "loading"; })\n      .addCase(fetchUser.fulfilled, (s, a) => { s.status = "succeeded"; s.data = a.payload; })\n      .addCase(fetchUser.rejected,  (s, a) => { s.status = "failed"; s.error = a.payload; });\n  },\n});\n```\nالـ thunk يُرسل ثلاثة actions تلقائيًا: pending وfulfilled وrejected.\n\nبدائل وسياق:\n• redux-saga: أقوى للتدفقات المعقّدة (إلغاء، تسلسل، مهام متوازية) لكنه منحنى تعلّم كبير.\n• redux-observable: مبني على RxJS.\n• RTK Query: للحالة الشائعة — جلب بيانات من API — وهو أبسط وأقل كودًا من كتابة thunks يدويًا.\n\nوالرأي العملي: إن كان معظم استخدامك للـ thunks هو جلب بيانات، فأنت تكتب يدويًا ما تفعله RTK Query أو React Query أفضل منك.',
      en: 'Middleware sits between dispatching an action and it reaching the reducer, and can intercept, modify, delay or block it.\n\n```js\nconst logger = (store) => (next) => (action) => {\n  console.group(action.type);\n  console.log("before:", store.getState());\n  const result = next(action);\n  console.log("after:", store.getState());\n  console.groupEnd();\n  return result;\n};\n```\n\nWhy is it needed for async work? Because a reducer must be a pure function — no network calls, no side effects. Middleware is the designated place for those.\n\nWith Redux Toolkit you use createAsyncThunk:\n```js\nexport const fetchUser = createAsyncThunk(\n  "users/fetch",\n  async (userId, { rejectWithValue }) => {\n    try {\n      return await api.getUser(userId);\n    } catch (error) {\n      return rejectWithValue(error.message);\n    }\n  }\n);\n\nconst usersSlice = createSlice({\n  name: "users",\n  initialState: { data: null, status: "idle", error: null },\n  reducers: {},\n  extraReducers: (builder) => {\n    builder\n      .addCase(fetchUser.pending,   (s) => { s.status = "loading"; })\n      .addCase(fetchUser.fulfilled, (s, a) => { s.status = "succeeded"; s.data = a.payload; })\n      .addCase(fetchUser.rejected,  (s, a) => { s.status = "failed"; s.error = a.payload; });\n  },\n});\n```\nThe thunk dispatches three actions automatically: pending, fulfilled and rejected.\n\nAlternatives and context:\n• redux-saga: more powerful for complex flows (cancellation, sequencing, parallel tasks) but a steep learning curve.\n• redux-observable: built on RxJS.\n• RTK Query: for the common case — fetching from an API — and far simpler than hand-written thunks.\n\nThe practical view: if most of your thunks are data fetching, you are hand-writing what RTK Query or React Query does better.',
    },
  },
  {
    id: 'state-normalization',
    difficulty: intermediate,
    question: {
      ar: 'ما هو تطبيع الحالة (State Normalization) ولماذا نحتاجه؟',
      en: 'What is state normalization and why do we need it?',
    },
    answer: {
      ar: 'التطبيع يعني تخزين البيانات في شكل مسطّح بمفاتيح معرّفات بدل شجرة متداخلة ومكرّرة — تمامًا كما في قواعد البيانات العلائقية.\n\nالمشكلة قبل التطبيع:\n```js\n{\n  posts: [\n    { id: 1, title: "أول", author: { id: 10, name: "Sara" } },\n    { id: 2, title: "ثانٍ", author: { id: 10, name: "Sara" } },\n  ]\n}\n```\nبيانات Sara مكرّرة. عند تغيير اسمها يجب تحديث كل نسخة، وإن نسيت واحدة ظهر التطبيق بحالتين متناقضتين.\n\nبعد التطبيع:\n```js\n{\n  posts: {\n    byId: {\n      1: { id: 1, title: "أول", authorId: 10 },\n      2: { id: 2, title: "ثانٍ", authorId: 10 },\n    },\n    allIds: [1, 2],\n  },\n  users: {\n    byId: { 10: { id: 10, name: "Sara" } },\n    allIds: [10],\n  },\n}\n```\n\nالمكاسب:\n1. مصدر واحد لكل كيان — التحديث في مكان واحد.\n2. وصول فوري بالمعرّف دون بحث في مصفوفة.\n3. تحديثات أبسط وأقل عرضة للأخطاء.\n4. عمليات إعادة تصيير أقل، لأن تغيير كيان لا يغيّر مرجع القائمة كاملة.\n\nالتكلفة: تحتاج selectors لإعادة تركيب البيانات للعرض، وكود أكثر قليلًا.\n\nمتى يستحق؟ عند وجود علاقات وتكرار وبيانات كثيرة تتغير. ولا يستحق لقائمة بسيطة تُعرض وتُستبدل كاملة.\n\nRedux Toolkit يوفّر createEntityAdapter الذي يتولى هذا النمط تلقائيًا مع دوال جاهزة للإضافة والتحديث والحذف والـ selectors.\n\nملاحظة سياقية: React Query تقلّل الحاجة لهذا كثيرًا، لأنها تدير الـ cache بمفاتيح استعلام وتتولى الإبطال بدلًا منك.',
      en: 'Normalization means storing data flat, keyed by id, instead of as a nested and duplicated tree — exactly as relational databases do.\n\nThe problem before normalizing:\n```js\n{\n  posts: [\n    { id: 1, title: "First", author: { id: 10, name: "Sara" } },\n    { id: 2, title: "Second", author: { id: 10, name: "Sara" } },\n  ]\n}\n```\nSara\'s data is duplicated. Renaming her means updating every copy, and missing one leaves the UI showing two contradictory states.\n\nAfter normalizing:\n```js\n{\n  posts: {\n    byId: {\n      1: { id: 1, title: "First", authorId: 10 },\n      2: { id: 2, title: "Second", authorId: 10 },\n    },\n    allIds: [1, 2],\n  },\n  users: {\n    byId: { 10: { id: 10, name: "Sara" } },\n    allIds: [10],\n  },\n}\n```\n\nThe gains:\n1. One source per entity — update in one place.\n2. O(1) access by id instead of searching an array.\n3. Simpler, less error-prone updates.\n4. Fewer re-renders, because changing one entity does not change the whole list\'s reference.\n\nThe cost: you need selectors to reassemble data for display, and a bit more code.\n\nWhen is it worth it? When you have relationships, duplication and a lot of changing data. It is not worth it for a simple list that is displayed and replaced wholesale.\n\nRedux Toolkit provides createEntityAdapter, which implements this pattern for you with ready-made add/update/remove operations and selectors.\n\nA contextual note: React Query reduces the need for this considerably, because it manages a cache keyed by query and handles invalidation for you.',
    },
  },
  {
    id: 'state-zustand',
    difficulty: intermediate,
    question: {
      ar: 'ما هي البدائل الحديثة لـ Redux؟ ومتى تختار Zustand أو Jotai؟',
      en: 'What are the modern alternatives to Redux, and when would you pick Zustand or Jotai?',
    },
    answer: {
      ar: 'ظهرت بدائل أخف بعد أن أصبح Redux يبدو ثقيلًا لكثير من الحالات:\n\n1. Zustand — مخزن واحد بواجهة بسيطة جدًا:\n```js\nconst useCartStore = create((set) => ({\n  items: [],\n  addItem: (item) => set((s) => ({ items: [...s.items, item] })),\n  clear: () => set({ items: [] }),\n}));\n\n// الاستخدام مع اشتراك انتقائي\nconst count = useCartStore((s) => s.items.length);\n```\nمميزاته: لا Provider، وكود قليل جدًا، واشتراك انتقائي افتراضي (لا يُعاد التصيير إلا عند تغيّر ما تختاره)، ويمكن استخدامه خارج مكوّنات React.\n\n2. Jotai — نهج ذري (atomic): وحدات حالة صغيرة مستقلة تتركّب:\n```js\nconst countAtom = atom(0);\nconst doubledAtom = atom((get) => get(countAtom) * 2);\n```\nمناسب عندما تكون الحالة مجزّأة بطبيعتها ومترابطة باشتقاقات. Recoil فكرة مشابهة.\n\n3. XState — آلات حالة، ممتازة عندما يكون المنطق تسلسلًا من الحالات بقواعد انتقال صارمة (معالج متعدد الخطوات، مشغّل وسائط).\n\nكيف أختار؟\n• Context: قيم بسيطة قليلة التغيّر.\n• Zustand: حالة عميل مشتركة، بسيط وكافٍ لمعظم الحالات — وهو خياري الافتراضي اليوم.\n• Redux Toolkit: فرق كبيرة، منطق معقّد، حاجة للأدوات والـ middleware ونمط موحّد مفروض.\n• Jotai: حالة ذرية مشتقّة.\n• XState: آلات حالة صريحة.\n• React Query: بيانات الخادم — وهذه ليست منافسة للسابقات بل مكمّلة.\n\nونصيحة عملية: في كثير من التطبيقات اليوم، React Query + useState + Context كافية تمامًا، ولا يظهر أي مخزن عام إلا متأخرًا وعند حاجة واضحة.',
      en: 'Lighter alternatives emerged once Redux started to feel heavy for many cases:\n\n1. Zustand — a single store with a very small API:\n```js\nconst useCartStore = create((set) => ({\n  items: [],\n  addItem: (item) => set((s) => ({ items: [...s.items, item] })),\n  clear: () => set({ items: [] }),\n}));\n\n// used with selective subscription\nconst count = useCartStore((s) => s.items.length);\n```\nIts advantages: no Provider, very little code, selective subscription by default (you only re-render when your selection changes), and it works outside React components.\n\n2. Jotai — an atomic approach: small independent units of state that compose:\n```js\nconst countAtom = atom(0);\nconst doubledAtom = atom((get) => get(countAtom) * 2);\n```\nA good fit when state is naturally granular and connected through derivations. Recoil is a similar idea.\n\n3. XState — state machines, excellent when the logic is a sequence of states with strict transition rules (a multi-step wizard, a media player).\n\nHow do I choose?\n• Context: simple, slow-changing values.\n• Zustand: shared client state, simple and sufficient for most cases — my default today.\n• Redux Toolkit: large teams, complex logic, a real need for devtools, middleware and an enforced pattern.\n• Jotai: atomic, derived state.\n• XState: explicit state machines.\n• React Query: server data — complementary to all of the above rather than competing.\n\nA practical note: in many applications today, React Query plus useState plus Context is entirely sufficient, and no global store appears until much later, if at all.',
    },
  },

  // ---------------------------------------------------------------- ADVANCED
  {
    id: 'state-architecture-advanced',
    difficulty: advanced,
    question: {
      ar: 'كيف تصمم معمارية الحالة في تطبيق كبير؟',
      en: 'How do you design state architecture in a large application?',
    },
    answer: {
      ar: 'أبدأ بتصنيف الحالة، لأن الخطأ المعماري الأشيع هو معاملة كل شيء بنفس الأداة:\n\n1. حالة الخادم → React Query أو RTK Query. تشمل كل ما مصدره API.\n2. حالة الـ URL → search params. الفلاتر والصفحة والترتيب والبحث. وضعها في useState خطأ شائع: يكسر زر الرجوع ويمنع مشاركة الرابط ويفقد الحالة عند التحديث.\n3. حالة النماذج → React Hook Form.\n4. حالة واجهة محلية → useState داخل المكوّن.\n5. حالة واجهة مشتركة → Context للبسيط، أو Zustand/Redux للمعقّد.\n\nالمبادئ التي أطبّقها:\n\n• أدنى مستوى ممكن: لا ترفع الحالة إلا عند الحاجة الفعلية، ولا تجعل العام افتراضًا.\n• مصدر واحد للحقيقة: لا تكرّر نفس البيانات في مكانين، فالتزامن بينهما دَين دائم.\n• لا تخزّن ما يمكن اشتقاقه: عدد العناصر المحددة يُحسب من القائمة، ولا يُخزَّن بجانبها.\n• اجعل الحالات المستحيلة غير قابلة للتمثيل: اتحاد مميَّز بدل عدة أعلام منطقية مستقلة.\n• طبّع البيانات المرتبطة عند وجود علاقات وتكرار.\n• حدود واضحة: كل ميزة تملك حالتها، والمشترك فقط يصعد إلى الأعلى.\n\nعلامات على معمارية سيئة:\n• useEffect لمزامنة حالتين معًا.\n• نفس البيانات في ثلاثة أماكن مختلفة.\n• تغيير بسيط يتطلب لمس خمسة ملفات في طبقات مختلفة.\n• لا أحد يعرف من يغيّر قيمة معيّنة.\n\nونقطة توثيقية مهمة: وثّق قرار "أين تعيش كل نوع من الحالة" في المشروع، وإلا اختلط كل شيء خلال أشهر مع نمو الفريق.',
      en: 'I start by classifying state, because the most common architectural mistake is treating everything with the same tool:\n\n1. Server state → React Query or RTK Query. Anything sourced from an API.\n2. URL state → search params. Filters, page, sort, search. Putting these in useState is a frequent error: it breaks the back button, prevents sharing links, and loses state on refresh.\n3. Form state → React Hook Form.\n4. Local UI state → useState inside the component.\n5. Shared UI state → Context for simple cases, Zustand or Redux for complex ones.\n\nThe principles I apply:\n\n• Lowest level possible: lift state only when genuinely needed, and never make global the default.\n• One source of truth: never duplicate the same data in two places — keeping them in sync is permanent debt.\n• Do not store what can be derived: the count of selected items comes from the list, it is not stored alongside it.\n• Make impossible states unrepresentable: a discriminated union instead of several independent booleans.\n• Normalise related data when you have relationships and duplication.\n• Clear boundaries: each feature owns its state, and only what is genuinely shared moves up.\n\nSigns of poor architecture:\n• useEffect used to keep two pieces of state in sync.\n• The same data living in three different places.\n• A small change requiring edits in five files across layers.\n• Nobody knowing what changes a given value.\n\nAnd an important documentation point: write down the decision about where each kind of state lives. Without it, everything blurs together within months as the team grows.',
    },
  },
  {
    id: 'state-scenario-optimistic',
    difficulty: advanced,
    question: {
      ar: 'سيناريو: كيف تنفّذ تحديثًا متفائلًا (optimistic update) بشكل صحيح؟',
      en: 'Scenario: how do you implement an optimistic update correctly?',
    },
    answer: {
      ar: 'التحديث المتفائل يعني تحديث الواجهة فورًا قبل تأكيد الخادم، فيبدو التطبيق لحظيًا. مثاله الكلاسيكي زر الإعجاب.\n\nما يجب أن يتضمنه التنفيذ الصحيح:\n\n```js\nconst mutation = useMutation({\n  mutationFn: toggleLike,\n\n  onMutate: async (postId) => {\n    // 1. إلغاء أي جلب جارٍ حتى لا يستبدل تحديثنا\n    await queryClient.cancelQueries({ queryKey: ["post", postId] });\n\n    // 2. حفظ نسخة للتراجع\n    const previous = queryClient.getQueryData(["post", postId]);\n\n    // 3. التحديث الفوري\n    queryClient.setQueryData(["post", postId], (old) => ({\n      ...old,\n      liked: !old.liked,\n      likes: old.likes + (old.liked ? -1 : 1),\n    }));\n\n    return { previous };\n  },\n\n  onError: (error, postId, context) => {\n    // 4. التراجع عند الفشل\n    queryClient.setQueryData(["post", postId], context.previous);\n    toast.error("تعذّر الحفظ");\n  },\n\n  onSettled: (data, error, postId) => {\n    // 5. المزامنة مع الخادم في كل الأحوال\n    queryClient.invalidateQueries({ queryKey: ["post", postId] });\n  },\n});\n```\n\nالخطوات الخمس كلها ضرورية، وإغفال أي منها يسبب خطأ حقيقيًا: إلغاء الطلبات الجارية يمنع استبدال تحديثك ببيانات قديمة، وحفظ النسخة يجعل التراجع ممكنًا، والإبطال في النهاية يصحّح أي فارق بين توقعك والنتيجة الفعلية.\n\nمتى يكون مناسبًا؟ للعمليات عالية الاحتمال للنجاح وقليلة الأثر: إعجاب، إضافة إلى مفضلة، تعليم مهمة كمنجزة، إعادة ترتيب قائمة.\n\nمتى لا يكون مناسبًا؟ للعمليات المالية أو التي قد تفشل لأسباب منطقية (رصيد غير كافٍ، تعارض، صلاحيات). إظهار نجاح ثم التراجع عنه أسوأ بكثير من انتظار ثانية.\n\nنقاط إضافية: تعامل مع عدة تحديثات متتابعة بسرعة (نقر متكرر)، وامنع حلقة التراجع من إرباك المستخدم برسائل متعددة، واجعل رسالة الفشل واضحة لا مجرد اختفاء صامت للتغيير.',
      en: 'An optimistic update means updating the UI immediately, before the server confirms, so the app feels instant. The classic example is a like button.\n\nWhat a correct implementation must include:\n\n```js\nconst mutation = useMutation({\n  mutationFn: toggleLike,\n\n  onMutate: async (postId) => {\n    // 1. cancel any in-flight fetch so it cannot overwrite our update\n    await queryClient.cancelQueries({ queryKey: ["post", postId] });\n\n    // 2. snapshot for rollback\n    const previous = queryClient.getQueryData(["post", postId]);\n\n    // 3. apply the update immediately\n    queryClient.setQueryData(["post", postId], (old) => ({\n      ...old,\n      liked: !old.liked,\n      likes: old.likes + (old.liked ? -1 : 1),\n    }));\n\n    return { previous };\n  },\n\n  onError: (error, postId, context) => {\n    // 4. roll back on failure\n    queryClient.setQueryData(["post", postId], context.previous);\n    toast.error("Could not save");\n  },\n\n  onSettled: (data, error, postId) => {\n    // 5. reconcile with the server either way\n    queryClient.invalidateQueries({ queryKey: ["post", postId] });\n  },\n});\n```\n\nAll five steps matter, and skipping any of them produces a real bug: cancelling in-flight queries stops stale data overwriting your update, the snapshot makes rollback possible, and the final invalidation corrects any difference between what you predicted and what actually happened.\n\nWhen is it appropriate? For operations very likely to succeed with low consequences: likes, favourites, marking a task done, reordering a list.\n\nWhen is it not? For financial operations, or anything that can fail for legitimate business reasons (insufficient funds, a conflict, permissions). Showing success and then retracting it is far worse than waiting a second.\n\nAdditional points: handle rapid repeated updates (double clicks), stop the rollback path from flooding the user with messages, and make the failure visible rather than letting the change silently disappear.',
    },
  },
  {
    id: 'state-scenario-refactor',
    difficulty: advanced,
    question: {
      ar: 'سيناريو: تطبيق يعاني من إدارة حالة فوضوية وبطء في الواجهة. كيف تعالجه؟',
      en: 'Scenario: an application has messy state management and a sluggish UI. How do you fix it?',
    },
    answer: {
      ar: 'أبدأ بالتشخيص لا بإعادة الكتابة، لأن "نعيد بناء إدارة الحالة" مشروع يُلغى في منتصفه عادة.\n\nالتشخيص:\n1. React DevTools Profiler مع Highlight updates: أي مكوّنات تُعاد ولماذا؟ عادة تظهر المشكلة فورًا — مكوّن عالٍ في الشجرة يُعاد تصييره مع كل ضغطة مفتاح.\n2. جرد الحالة: ما هي أنواع الحالة الموجودة وأين تعيش؟ غالبًا تجد بيانات خادم في Redux، وحالة فلاتر في useState بدل الـ URL، ونفس البيانات مكرّرة.\n3. ابحث عن العلامات: useEffect يزامن حالتين، ومخزن عام يحتوي أشياء يستخدمها مكوّن واحد.\n\nالمعالجة بالترتيب — من الأعلى أثرًا إلى الأقل:\n\n1. انقل بيانات الخادم إلى React Query. هذه عادة أكبر مكسب على الإطلاق: تحذف آلاف الأسطر، وتزيل طلبات مكررة، وتقلّل إعادة التصيير بشكل ملموس. نفّذها تدريجيًا، شاشة شاشة.\n\n2. انقل حالة الفلاتر والصفحات إلى الـ URL. مكسب مزدوج: أداء أفضل وتجربة أفضل (روابط قابلة للمشاركة وزر رجوع يعمل).\n\n3. اخفض الحالة المتبقية إلى أدنى مستوى ممكن. كثير من البطء سببه حالة موضوعة في App بلا داعٍ.\n\n4. قسّم الـ Contexts الكبيرة حسب معدل التغيّر، وافصل القيم عن دوال التحديث.\n\n5. احذف الحالة المشتقّة واحسبها أثناء التصيير.\n\n6. فقط بعد كل ما سبق: memo وuseMemo وuseCallback حيث يثبت القياس الحاجة.\n\nقواعد للتنفيذ:\n• تغييرات تدريجية قابلة للنشر، لا إعادة كتابة شاملة.\n• قِس قبل وبعد كل خطوة بأرقام حقيقية.\n• ابدأ بالشاشة الأكثر استخدامًا أو الأكثر بطئًا.\n• وثّق القواعد الجديدة، وإلا عادت الفوضى مع أول ميزة جديدة.',
      en: 'I start with diagnosis rather than a rewrite, because "let\'s redo state management" is a project that usually gets abandoned halfway.\n\nDiagnosis:\n1. React DevTools Profiler with Highlight updates: which components re-render and why? The problem usually appears immediately — a component high in the tree re-rendering on every keystroke.\n2. Inventory the state: what kinds exist and where do they live? You typically find server data in Redux, filter state in useState instead of the URL, and the same data duplicated.\n3. Look for the tells: a useEffect syncing two pieces of state, a global store holding things only one component uses.\n\nThe fixes, ordered by impact:\n\n1. Move server data to React Query. This is usually the single biggest win: it deletes thousands of lines, removes duplicate requests and measurably cuts re-renders. Do it incrementally, screen by screen.\n\n2. Move filter and pagination state into the URL. A double win: better performance and a better experience (shareable links and a working back button).\n\n3. Push the remaining state down to the lowest level. A lot of sluggishness comes from state sitting in App for no reason.\n\n4. Split large contexts by change frequency, and separate values from updater functions.\n\n5. Delete derived state and compute it during render.\n\n6. Only after all of the above: memo, useMemo and useCallback where measurement proves they are needed.\n\nRules for execution:\n• Incremental, shippable changes, not a big-bang rewrite.\n• Measure before and after each step with real numbers.\n• Start with the most-used or slowest screen.\n• Document the new rules, or the mess returns with the next feature.',
    },
  },
];
